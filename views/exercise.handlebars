  <div class="container">
      <h1 class="blue-text text-darken-2">Welcome to FitSync</h1>
      <p>THIS IS DIFFERENT THAN THE OTHER</p>

      <ul>
        {{#each exercises as |exercise|}}
          <li>
            <h3>{{exercise.name}}</h3>
            <p>{{exercise.description}}</p>
            <p>Author: {{exercise.User.name}}</p>
          </li>
        {{/each}}
      </ul>
</div>
 